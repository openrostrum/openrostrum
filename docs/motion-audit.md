# Motion audit

The decision unit is a surface family: every concrete implementation and consumer is named in the Files column. “Static” is an intentional outcome, not an omission.

| Surface | Files | Before | After / decision | Timing + easing | Why |
| --- | --- | --- | --- | --- | --- |
| Motion vocabulary | `app/app.css`; `app/ui/motion.tsx` | Numeric durations and built-in easing were scattered across components. | **Changed:** semantic feedback/enter durations and responsive/settle curves; one source for reveal, popover, and dialog physics. | 120ms responsive; 180ms settle | A quiet product needs one physical vocabulary, not per-component taste. |
| Primary/ghost buttons and links | `app/ui/button.tsx`; all `Button`/`ButtonLink` consumers | 150ms background/press feedback. | **Changed:** same parallel hover/press feedback, tokenized and reduced-motion safe. | 120ms responsive | Press feedback confirms input without gating its outcome. |
| Shared prose/navigation hover | `app/ui/text-link.tsx`; `app/ui/table.tsx`; `app/ui/tabs.tsx`; `app/ui/shell.tsx`; `app/components/portal/task-detail-view.tsx` | Some 150ms fades; some abrupt color changes. | **Changed:** color feedback uses the shared token; content/state changes remain immediate. | 120ms responsive | A color fade runs in parallel and costs no task time. |
| Event switcher popover | `app/components/event-switcher.tsx` | Conditionally mounted panel popped in. | **Changed:** trigger feedback is tokenized; anchored panel settles from its top edge. Dismissal stays instant. | 180ms settle; 120ms row feedback | The panel is occasional and spatially belongs to its trigger. |
| Theme menu popover | `app/components/theme-toggle.tsx` | Conditionally mounted panel popped in above its trigger. | **Changed:** panel settles from bottom-right; form-submit close behavior remains intact; palette changes instantly. | 180ms settle; 120ms row feedback | The menu can move; the whole page palette should not become a spectacle. |
| Submission-form actions menu | `app/routes/admin.forms.tsx` | Native `details` opened a hand-styled panel instantly. | **Changed:** route composes `PopoverSurface`; panel settles from top-right. | 180ms settle | This is a true trigger-anchored menu; motion clarifies the attachment. |
| Decision-email preview modal | `app/ui/modal.tsx`; `app/components/decision-preview-dialog.tsx`; `app/routes/admin.submissions.tsx` | Shared modal appeared with no transition. | **Changed:** backdrop fades while the centered panel settles from 0.97 scale. Closing remains instant. | 180ms settle | Email review is occasional; content is interactive from mount, so motion adds no wait. |
| Delete-form alert dialog | `app/routes/admin.forms.tsx` | Route-owned fixed dialog popped in. | **Changed:** promoted to `DialogSurface`, centered settle; Escape/cancel/delete semantics preserved. | 180ms settle | Destructive confirmation benefits from a clear layer change without a slow dismissal. |
| CFP destructive confirm | `app/cfp/ui.tsx`; public draft/withdraw consumers | Public alert dialog popped in. | **Changed:** shared alert-dialog surface and centered settle; Escape and confirm node unchanged. | 180ms settle | Occasional destructive state deserves layer continuity. |
| Agenda publish confirmation | `app/agenda/board.tsx`; `app/routes/admin.agenda.tsx` | Hand-rolled fixed alert dialog popped in. | **Changed:** promoted to `DialogSurface`; focus trap/return, conflict preview, Escape, and publish behavior preserved. | 180ms settle | Publishing is occasional; centered modal physics fit a viewport-level decision. |
| Add-submission/session panel | `app/lib/submission-list.tsx`; Abstracts/Sessions pages | Full-width “drawer” panel mounted abruptly. | **Changed:** content gets a 2px opacity/translate reveal; layout becomes available immediately and close is instant. | 180ms settle | The panel is occasional, but animating height would gate and jank the list. |
| Rich-text link controls | `app/ui/rich-text.tsx`; all shared editor consumers | Link row mounted abruptly. | **Changed:** short inline reveal; input is usable immediately. | 180ms settle | A small spatial reveal connects the Link button to its controls without delaying typing. |
| Shared two-step confirmation | `app/ui/confirm-button.tsx`; portal participation/removal consumers | Prompt/buttons replaced the initial control abruptly. | **Changed:** confirmation content reveals in place; cancel remains instant. | 180ms settle | The guard is occasional; the state should feel connected, but safety copy must be readable immediately. |
| Clipboard acknowledgement | `app/components/copy-button.tsx`; API keys, invite links, form links, embeds | Button label swapped instantly. | **Changed:** success/failure label reveals inside the already-completed button action. | 180ms settle | Pure parallel feedback is the highest-value, lowest-cost motion. |
| Per-session calendar acknowledgement | `app/components/add-to-calendar.tsx`; public session details | Download message appeared abruptly. | **Changed:** inline acknowledgement reveals after navigation to the `.ics` resource starts. | 180ms settle | The download does not wait for the message. |
| Itinerary export acknowledgement | `app/widgets/surfaces.tsx` | Export message appeared abruptly. | **Changed:** same shared feedback reveal as per-session downloads. | 180ms settle | Identical outcomes should feel identical across public surfaces. |
| File-picker hover | `app/components/file-picker.tsx` | Drop-zone hover snapped. | **Changed:** tokenized background fade; filename replacement remains static. | 120ms responsive | Hover is parallel; selected filename is information and should update immediately. |
| Rich-text toolbar buttons | `app/ui/rich-text.tsx` | Hover/active colors snapped. | **Changed:** hover colors fade; formatting mark state and editing remain immediate. | 120ms responsive | Visual acknowledgement can be smooth while editor commands stay direct. |
| Portal pill toggles | `app/components/portal/bits.tsx`; `portal/home-view.tsx` | Task-tab pills snapped between states. | **Changed:** press/color feedback is parallel; task content swaps immediately. | 120ms responsive | The button can feel pressed without animating the frequently switched content. |
| Personal-schedule star | `app/widgets/bits.tsx`; itinerary/session cards | Star state snapped with no press feedback. | **Changed:** tokenized press/color feedback; localStorage state changes immediately. | 120ms responsive | Direct acknowledgement costs no time and makes the control feel physical. |
| Public speaker/session hover | `app/widgets/surfaces.tsx` | Directory rows and agenda blocks changed background abruptly. | **Changed:** background color fades only. | 120ms responsive | Pointer wayfinding is smoother without moving program data. |
| Marketing navigation/cards/CTA | `app/marketing/landing.tsx`; `app/marketing/auth.tsx`; `app/marketing/primitives.tsx` | Mixed implicit, 150ms, and 160ms timing. | **Changed:** all parallel hover/press feedback uses design tokens and reduced-motion fallbacks. | 120ms responsive | The doorway should share product physics even at a larger visual scale. |
| Marketing product shot first paint | `app/marketing/landing.tsx` | Admin mock entered over 500ms from 12px below. | **Changed to static:** removed. | — | It was consciously watched and delayed the product screenshot for no explanatory value. |
| Tabs and route content | `app/ui/tabs.tsx`; admin tabs; portal nav; public day/view tabs | Active color faded while route content swapped. | **Static content:** preserve instant navigation/content; only parallel color feedback moves. | 120ms color only | Animating content would compound in all-day navigation and obscure loading truth. |
| Search, filters, pagination, result counts | `app/components/portal/submissions-view.tsx`; `app/routes/admin.contacts.tsx`; `app/routes/admin.crm.directory.tsx`; `app/lib/submission-list.tsx`; `app/widgets/filter-bar.tsx`; `app/widgets/bits.tsx`; list routes | Query/results update or navigate directly. | **Static.** | — | Search outcomes are the work; they must never wait for decoration. |
| Table selection and inline statuses | `app/ui/table.tsx`; `app/lib/submission-list.tsx`; `app/routes/admin.files.tsx`; `admin.crm.directory.tsx`; `admin.reviewers.tsx`; evaluation/settings tables | Selection/status state changes immediately with a color wash. | **Static data; tokenized row hover only.** | 120ms hover only | Selection can be keyboard-driven and repeated; data truth must be immediate. |
| Form-builder steps and type cards | `app/routes/admin.forms.$formId.tsx` (`step`, `formType`, participant roles) | Panels and selected cards swap immediately. | **Static.** | — | Organizers iterate rapidly; step/content animation would add cumulative wait. |
| Form-builder rule/editor panels | `app/routes/admin.forms.$formId.tsx` (`openRule`, field search/type/mode, create/edit panels) | Conditional editors mount/unmount immediately. | **Static.** | — | These reflow dense configuration content; opacity would not solve the layout jump and height animation would jank. |
| Public conditional questions and wizard validation | `app/routes/submit.$eventSlug.$formId.tsx`; `.step.session.tsx`; `.step.participant.tsx` | Fields/errors appear from local state. | **Static.** | — | Conditional fields carry a sub-100ms experience contract; errors must never arrive late. |
| Auth/onboarding validation and slug derivation | `login.tsx`; `signup.tsx`; `forgot-password.tsx`; `set-password.$token.tsx`; `onboarding.tsx`; `app/settings/event-form.tsx` | Errors/derived values replace in place. | **Static.** | — | These states explain whether progress is possible; immediate clarity beats motion. |
| Evaluation editors/details/guards | `app/routes/admin.evaluation.tsx`; `admin.evaluation.$planId.tsx`; `reviews.$id.tsx` | Round/question editors, detail rows, delete/abstain prompts mount inline. | **Static.** | — | Dense reviewer workflows are frequent; most surfaces reflow tables or forms and should stay direct. |
| Task editors/reminder/delete/assignment controls | `app/routes/admin.tasks.tsx`; `admin.tasks_.$assignmentId.tsx`; portal task detail | Inline panels and response state change directly. | **Static except shared `ConfirmButton`.** | — | Task triage is repeated; shared guard feedback is enough without animating data. |
| Settings/library/team/portal-form/email/embed guards | `admin.settings.library.tsx`; `admin.settings.team.tsx`; `admin.portal-forms.tsx`; `admin.emails.tsx`; `admin.embeds.tsx` | Editors and inline confirmations replace rows/content. | **Static.** | — | These reflow surrounding content and expose safety copy; delayed readability would be counterproductive. |
| Contacts/CRM/import/files local state | `admin.contacts.tsx`; `admin.contacts_.import.tsx`; `admin.crm.directory.tsx`; `admin.files.tsx`; `admin.files_.$id.tsx`; `app/components/crm-notes.tsx` | Search, selection, mapping, notes, and comment drafts update directly. | **Static.** | — | High-volume record work should feel immediate; hover/press primitives already provide acknowledgement. |
| Submission detail guards and preview recipient | `admin.submissions_.$id.tsx`; `admin.submissions.tsx`; `app/components/decision-preview-dialog.tsx` | Delete guard and recipient selection change content directly. | **Static content inside animated modal.** | — | The layer transition is enough; changing recipients and confirming deletion should not add nested motion. |
| Portal search/task tabs/profile/comment drafts | `app/components/portal/home-view.tsx`; `submissions-view.tsx`; `profile-view.tsx`; `task-detail-view.tsx` | Local state changes content/forms immediately. | **Static content; pill/link feedback only.** | 120ms control feedback only | Speaker work should remain phone-fast and predictable. |
| Agenda filter chips and inline placement | `app/agenda/board.tsx` (`FilterChip`, `PlaceInline`, `ToggleChips`) | Filter/placement controls changed directly; chip had 150ms color fade. | **Static:** removed chip transition; placement form mounts immediately. | — | These controls are part of direct schedule manipulation and keyboard fallback; input coupling wins. |
| Agenda drag/drop and conflict feedback | `app/agenda/board.tsx`; `app/routes/admin.agenda.tsx` | dnd-kit directly transforms dragged geometry; `dropAnimation={null}`; slots/conflicts update instantly. | **Static by design; no decorative settle.** | Input-coupled transform only | Any extra easing would detach the session from the pointer/keyboard and weaken precision. |
| Show-more text | `app/widgets/bits.tsx` | Text reflows instantly on expand/collapse. | **Static.** | — | Height is content-dependent; layout animation would be watched and expensive. |
| File names, counters, toolbar marks | `app/components/file-picker.tsx`; `app/components/rich-text.tsx`; `app/cfp/ui.tsx`; `app/ui/rich-text.tsx` | Text/mark state changes immediately. | **Static information.** | — | These are direct, frequently checked values; fading them would reduce certainty. |
| Notices, alerts, server errors, empty states | `app/components/portal/bits.tsx`; `app/cfp/ui.tsx`; route `ErrorText`/action notices; `app/ui/empty-state.tsx` | Inline information appears with the data/render that produced it. No standalone toast primitive exists. | **Static except explicit copy/download acknowledgements above.** | — | Errors and gating notices must be readable immediately; pretending all notices are toasts would hide durable information. |
| Theme palette switch | `app/components/theme-toggle.tsx`; root theme cookie/optimistic fetcher | Document colors change immediately. | **Static palette; animated menu only.** | — | Whole-page interpolation is conspicuous and can reduce contrast mid-transition. |
| Lazy editor/loading/empty swaps | `app/ui/rich-text-lazy.tsx`; `app/cfp/ui.tsx`; route Suspense/loading states | Fallback/editor and empty/data content replace directly. | **Static.** | — | Motion must not become loading theater or disguise hydration cost. |
| Skeleton loading | `app/ui/skeleton.tsx` | Existing pulse with reduced-motion fallback. | **Unchanged.** | Existing Tailwind pulse | It preserves page shape; the fallback already removes motion. |

## Totals

- **23 changed surface families:** 22 gained/standardized quiet parallel motion; 1 consciously watched marketing entrance was removed.
- **21 deliberately static surface families:** direct manipulation, data truth, dense reflow, validation, loading, and frequent navigation stay immediate.
- **No dependency, keyframe, delayed unmount, or JS animation layer was added.**
