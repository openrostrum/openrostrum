/** Secondary type at the email surfaces' scale — ~/ui has no equivalent. */
export function Hint({ children }: { children: React.ReactNode }) {
	return <span className="text-[12.5px] text-fg-muted">{children}</span>;
}
